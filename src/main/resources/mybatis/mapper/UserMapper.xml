<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wheatek.lockserver.mapper.UserMapper">
   <!--
       @Select("select * from user where phone_number = #{phoneNumber}")
        public User getUserByPhoneNumber(String phoneNumber);

        @Delete("delete from user where phone_number = #{phoneNumber}")
        public int deleteUserByPhoneNumber(String phoneNumber);

        @Insert("insert into user(user_name,phone_number) values (#{userName},#{phoneNumber})")
        public int insertUser(User user);

        @Update("update user(user_name) values (#{userName}) where phone_number = #{phoneNumber}")
        public int updateUser(User user);
    -->
    <select id="getUserByPhoneNumber" resultType="com.wheatek.lockserver.bean.User">
        select * from user where phone_number = #{phoneNumber}
    </select>

    <insert id="insertUser">
        insert into user(user_name,phone_number) values (#{userName},#{phoneNumber})
    </insert>

    <update id="updateUser" >
        update user set user_name = #{userName} where phone_number = #{phoneNumber}
    </update>

    <delete id="deleteUserByPhoneNumber" >
        delete from user where phone_number = #{phoneNumber}
    </delete>
</mapper>